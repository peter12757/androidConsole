// Top-level build file where you can add configuration options common to all sub-projects/modules.
buildscript {
    repositories {
        mavenCentral()
        google() // or jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.2.0'   //last version Jan 2016
    }
}